# Copie para .env e preencha. .env é ignorado pelo git — não versione credencial.
# Banco onboarding (schema modelo de referência).
DB_HOST=
DB_PORT=3306
DB_NAME=onboarding
DB_USER=
DB_PASSWORD=
DB_DRIVER=pdo_mysql
